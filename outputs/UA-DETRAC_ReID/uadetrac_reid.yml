MODEL:
  PRETRAINED: True
 
INPUT:
  SIZE_TRAIN: [224, 224]
  SIZE_TEST: [224, 224]
  PIXEL_MEAN: [0.485, 0.456, 0.406]
  PIXEL_STD: [0.229, 0.224, 0.225]
  PROB: 0.5 # random horizontal flip
  RE_PROB: 0.5 # random erasing 
  PADDING: 0

DATASETS:
  NAMES: 'uadetrac_reid'
  DATA_PATH: 'E:/datasets/UA-DETRAC_ReID'
  TRAIN_PATH: 'image_train'
  QUERY_PATH: 'image_query'
  GALLERY_PATH: 'image_test'

DATALOADER:
  SAMPLER: 'softmax_triplet'
  NUM_INSTANCE: 8
  NUM_WORKERS: 4

SOLVER:
  OPTIMIZER_NAME: 'Adam'
  MAX_EPOCHS: 70
  BASE_LR: 0.0001
  BIAS_LR_FACTOR: 1
  WEIGHT_DECAY: 0.0005
  WEIGHT_DECAY_BIAS: 0.0005
  IMS_PER_BATCH: 16
  FP16: True

  STEPS: [5, 10, 20, 40, 60]
  GAMMA: 0.6

  WARMUP_FACTOR: 0.01
  WARMUP_ITERS: 8
  WARMUP_METHOD: 'linear'

  CHECKPOINT_PERIOD: 10
  LOG_PERIOD: 100  # Number of batches between logs.
  EVAL_PERIOD: 10

  CYTHON: True

TEST:
  IMS_PER_BATCH: 16
  DEBUG: True
  WEIGHT: "./outputs/UA-DETRAC_ReID/resnet50_epoch70.pth"

OUTPUT_DIR: "./outputs/UA-DETRAC_ReID"
